#include <iostream> 

int main() {
	int a[10];
	for (int i = 0; i < 10; ++i) {
		cin<<n;
		while (n >= 0 || n < 2) {
			cout << "Range is 0 to 1. Re-enter.";
			 cin<<n;
		}
		a[i] = n;
	}
	
	system("pause");
	reurn = 0;
}